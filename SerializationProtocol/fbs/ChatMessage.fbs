include "Player.fbs";

namespace SerializationProtocolFlatbuffer;

table ChatMessage {
  sender:Player;
  message:string;
  sent_timestamp:ulong; // Ms count since epoch begins
  received_timestamp:ulong;
}

root_type ChatMessage;